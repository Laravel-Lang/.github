{
  "CHECKS": {
    "regexp": "^(Draft:\\s)?\\[[a-zA-Z,_\\s]+\\]:\\s.+$",
    "ignoreLabels": ["skip-changelog", "dependabot"]
  },
  "MESSAGES": {
    "success": "PR Title format was accepted",
    "failure": "The PR title does not match the format: /^(Draft:\\s)?\\[[a-zA-Z,_\\s]+\\]:\\s.+$/"
  }
}
