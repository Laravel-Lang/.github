{
  "CHECKS": {
    "regexp": "^\\[[a-z]{2,3}[a-zA-Z_]*\\]:\\s.+$"
  },
  "MESSAGES": {
    "success": "PR Title format was accepted",
    "failure": "The PR title does not match the format: /^\\[[a-z]{2,3}[a-zA-Z_]*\\]:\\s.+$/"
  }
}
